
# 1 CAP 

![[201_Scaliability_Engineering/image/Pasted image 20250429164559.png]]

A SYSTEM is partition into 2 data center (Replicated System)

P: Partition Tolerance, Tolerance  to network Communication between data replicas	
即使系统的某些部分之间网络通信失败（网络分区），系统仍能继续运行
 not data partitioning/ sharding 

设想一个系统被分为两个数据中心 A 和 B，中间网络断了（网络分区），这时：

    如果你选择保留 P + A：系统继续响应请求，但不同数据中心之间可能不一致（牺牲一致性 C）。

    如果你选择保留 P + C：系统保证一致性，但有些请求可能超时或失败（牺牲可用性 A）。

---

![[201_Scaliability_Engineering/image/Pasted image 20250429165140.png]]


Choose Consistency , wait until all 2 replica were updated 
Chose Latency, 不等到 第二个 Replica 被更新, only the first 被更新就行了 

low latenct 更重要 , 比起 consistency 


# 2 Causal Consistency

cusual consistency , 反义词 是 prallel consistency 
causalilty 


R/W operation ist cusually-related , not in arbitrary oder, muss follow in a specific oder, in cusual consistency  

Monotonic Reads: A read will never return older values than previously returned to the same client. read already, then momery it forver , never forget 
Read Your Writes: A read will never return older values than previously written by the same client. 被修改过后, 修改前的值 就不会被返回了, 
Write Follows Reads: A client that reads version X and then updates the same data item, will only update replicas that have at least version X.
Monotonic Writes: Two updates of the same client will always be serialized corresponding to the chronological order of their submission. (every system obeys this rules , never violates this )

effects on an email application 

No monotonic reads:
“When reloading your email inbox, previously seen emails will suddenly disappear.“

No read your writes:
“When you open the sent folder, the email you just sent may not be there.“

  faNo monotonic writes:
“After creating an email draft, you remove person X from the list of addressees. When you send
the email you realize that the email was sent to the old list of recipients – including person X.“



# 3 MISC




# 1 AJAX: Dynamic web pages: 

重要!: 
AJAX makes web applications more responsive by
i) fetching small pieces of information asynchronously in the background,
ii) updating the DOM tree upon receipt
instead of reloading the entire page.



• AJAX allows Web sites to exchange data with a server without having to reload the entire Web page (it acts ‘behind the scenes’).
• Users do not wait for the entire page to load as some data is already at the client.
• This is intended to increase the web page's interactivity, speed, functionality and usability.

• AJAX is an acronym for ‘Asynchronous JavaScript and XML’
• AJAX is a group of inter-related Web development techniques used for creating interactive web applications:
XML, (X)HTML and CSS for rendering information
DOM for interacting with the information
JavaScript gluing everything together
XMLHttpRequest (XHR) object for retrieving JSON/XML data asynchronously from the web server


![[400_Anwendungssystem/image/Pasted image 20250627210024.png]]


## 1.1 Asynchronous processing in AJAX

1. Kick off an HTTP request performed in background via XMLHttpRequest object
2. XMLHttpRequest asynchronously contacts server and retrieves data
3. Server retrieves data and sends it back
	1. Can take any amount of time
4. Event listener used to detect when the XMLHttpRequest finished retrieving data
	1. Listens for changes in “readyState” variable
5. Callback into JavaScript code

![[400_Anwendungssystem/image/Pasted image 20250627210119.png]]


![[400_Anwendungssystem/image/Pasted image 20250627210436.png]]


## 1.2 Example 

![[400_Anwendungssystem/image/Pasted image 20250627210728.png]]



## 1.3 Usage 

Rich Client for Web 2.0 / Rich Internet Application (RIA)
• For example, controls (such as trees, menus, or progress bars) can be provided without page refreshes; drag-and-drop can be supported; etc.

Client-side data validation
• (Form) data that would require server-side validation can be validated directly at the client side instead of submitting it to the server

Auto completion
• Parts of (form) data can be auto-completed while the user is typing 

Master-data retrieval
• Based on a client event, more detailed information related to data already at client side can be fetched “in the background” without refreshing complete page


## 1.4 Pros and cons of using AJAX

Advantages

Seamless HTML integration
AJAX is not something embedded within HTML
No separate markup language interwoven with HTML

Standards-based cross-browser support
AJAX works with nearly every browser
No plug-ins required for AJAX

Backend agnostic
AJAX works with every HTTP-compliant
backend, e.g. Java (EE/SE), PHP, ASP, .NET,….


Distribution
Other RIA technologies (Adobe Flash, Microsoft Silverlight,…) require special backends, HTML or browser plug-ins


---

Disadvantages

Usability
Breaking the “back” button: Browsers cache static page only
addressed in HTML 5 Bookmarking is difficult because the client controls state and generates page, not the server

Performance
Number of server calls may increase Many fine-grained asynchronous calls instead of few coarse-grained calls

Security
Cross-origin resource sharing (CORS) prohibited (e.g., you cannot load data from http://www.x.com from a client that is hosted on http://www.y.com - addressed by XMLHttpRequest 2.0)

# 2 一些名词 


---

Typescript
Typescript is a programming language that transpiles to JavaScript. It is also a superset of
JavaScript, so that all valid JavaScript programs are also TypeScript programs.
It adds the following features:
- Type annotations with compile-time type checking
- Type inference and erasure
- Interfaces, Enums, Generics
- …

```
function add(left: number, right: number): number {
	return left + right;
}
```



----

Web Assembly (Wasm)

WebAssembly is a portable format for executable programs. All modern browsers have support for it, and it can be significantly faster than JavaScript as it does not have to be interpreted.

Developers usually write their software in another language (e.g., TypeScript, Kotlin, Rust), which is then compiled to WebAssembly.

---

Single Page Applications

A single page application is a web application that does not direct users to new websites, but rewrites the current DOM dynamically. SPAs heavily use JavaScript, CSS, and new HTML features. This allows developers to recreate desktop-like applications on the web


----


Progressive Web Apps are a special type of website that adheres to additional standards.

Some browsers (e.g., Chromium-based Browsers, Safari, Firefox Android) can then “install” these applications, so that they also work without an Internet connection and behave similar to native apps.

Developers only need to write their application once and can deliver a native-feeling experience on multiple platforms


---
Electron

Electron is an open-source software framework to create desktop applications using web technologies.

It uses a version of the Chromium browser engine with various additional APIs to enable desktop-like functionality.

The SpaceX Crew Dragon Touchscreen Interface is completely based on a similar technology.



---

Mobile apps

Modern web applications are often not only useable via the browser, but also have a mobile application that is distributed using an app store. These mobile apps often communicate with backend services using Web APIs, but are “thick” clients offering more features beyond the Web APIs.

Mobile apps:
- Run faster than websites  Can perform more complex calculations (e.g., animations or ML)
- They can have functionality even if the device has no Internet access (e.g., caching)
- Can use more features of the device (e.g., GPS for mobility services, push notifications)

Android (and LineageOS) apps are usually written in Java or Kotlin, iOS apps in Objective-C or Swift








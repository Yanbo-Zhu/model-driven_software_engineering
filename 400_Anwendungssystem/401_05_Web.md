

Participants are able to
• understand and discuss HTTP
• design RESTful interactions and invoke REST APIs from Java
• read SOAP messages and WSDL documents and understand the resulting interactions
• discuss architectural questions for web applications including concepts such as AJAX
• follow basic discussions on various web technologies (to the degree that they are presented in this course)



# 1 HTTP

![[image/Pasted image 20250617124356.png]]


How do these components communicate?

HTTP is a request-response, application layer protocol and a standard for client-server
communication
In HTTP, a client (e.g., a web browser) submits HTTP requests to a server; the server (which
stores content (or resources => REST) or generates content) sends messages back to the client
in response


![[image/Pasted image 20250617124515.png]]


## 1.1 HTTP requests and responses

Typically, an HTTP client establishes a TCP connection to a particular port on a host (port 80 by
default)
An HTTP server listening on that port waits for the client to send a request message
Upon receiving the request, the server sends back a status line, such as "HTTP/1.1 200 OK",
and a message of its own the body of which is perhaps the requested resource, an error
message or some other information.

![[image/Pasted image 20250617124856.png]]

---

## 1.2 HTTP status code categories

1xx: Informational
Request received, continuing process
2xx: Success
The action was successfully received, understood, and accepted
3xx: Redirection
Further action must be taken in order to complete the request
4xx: Client Error
The request contains bad syntax or cannot be fulfilled
5xx: Server Error
The server failed to fulfill an apparently valid request

Full list of status codes
1xx Informational
100 Continue
101 Switching Protocols
102 Processing
2xx Success
200 OK
201 Created
202 Accepted
203 Non-authoritative Information
204 No Content
205 Reset Content
206 Partial Content
207 Multi-Status
208 Already Reported
226 IM Used
3xx Redirection
300 Multiple Choices
301 Moved Permanently
302 Found
303 See Other
304 Not Modified
305 Use Proxy
307 Temporary Redirect
308 Permanent Redirect
4xx Client Error
400 Bad Request
401 Unauthorized
402 Payment Required
403 Forbidden
404 Not Found
405 Method Not Allowed
406 Not Acceptable
407 Proxy Authentication Required
408 Request Timeout
409 Conflict
410 Gone
411 Length Required
412 Precondition Failed
413 Payload Too Large
414 Request-URI Too Long
415 Unsupported Media Type
416 Requested Range Not Satisfiable
417 Expectation Failed
418 I'm a teapot
421 Misdirected Request
422 Unprocessable Entity
423 Locked
424 Failed Dependency
426 Upgrade Required
428 Precondition Required
429 Too Many Requests
431 Request Header Fields Too Large
444 Connection Closed Without Response
451 Unavailable For Legal Reasons
499 Client Closed Request
5xx Server Error
500 Internal Server Error
501 Not Implemented
502 Bad Gateway
503 Service Unavailable
504 Gateway Timeout
505 HTTP Version Not Supported
506 Variant Also Negotiates
507 Insufficient Storage
508 Loop Detected
510 Not Extended
511 Network Authentication Required
599 Network Connect Timeout Error


## 1.3 Resource identification (URI vs. URL vs. URN)

Resources to be accessed by HTTP are identified using
Uniform Resource Identifiers (URIs) — or, more specifically,
Uniform Resource Locators (URLs) — using the http: or https: URI schemes.

URI/URL use the same format but
• URLs specify a resource that can be fetched
• URIs are only identifiers (e.g., for XML namespaces), i.e., a request to a URI may result in an
error or return an empty document etc.

![[image/Pasted image 20250617125031.png]]


URI: A reference identifying an abstract or physical resource
• Form: `<scheme>://<authority><path>?<query>`
• Can be a URL, URN, or both
- URI 是一个**统称**，用于标识互联网上的资源。它可以用来**定位（URL）** 或 **命名（URN）** 一项资源。

URL: The subset of URIs that identifies resources by their primary access mechanism (e.g.
network location) 
URL 是 URI 的一个子集，用于**定位**资源（即告诉我们如何访问资源，通常包含协议和地址）。
这是我们最常用的类型（浏览器地址栏中的就是 URL）。
• Form = transport://user:password@host:port/path[?search][#fragmentid]
• Is a physical address of a resource
• Example: https://en.wikipedia.org/wiki/Uniform_Resource_Name#URIs,_URNs,_and_URLs

URN: The subset of URIs that uniquely identify a resource independent of its primary storage location
URN 也是 URI 的子集，用于**命名**资源，但不提供访问方式。它的目标是为资源提供一个**持久、唯一的名字**。
这些 URN 表示“某个资源”，但并不能告诉你在哪里能找到它。
• ==Identifier remains stable even is the resource is moved to a different physical location==
• Is a logical address of a resource
• Example: urn:isbn:0451450523


![[image/Pasted image 20250617125134.png]]



## 1.4 HTTP request messages


HTTP request messages consists of the following:
Request line, such as GET /images/logo.gif HTTP/1.1 which requests a resource called /images/logo.gif from the server


Headers, such as Accept-Language: en
An empty line
An optional message body



## 1.5 Request methods

HTTP defines eight methods ("verbs") indicating the desired action to be performed on the identified logical resource

What the resource represents, whether it is pre-existing data or data that is generated dynamically depends on the implementation of the server. Often, the resource corresponds to a file or the output of an executable residing on the server.


• HEAD
• GET
• POST
• PUT
• DELETE
• TRACE
• OPTIONS
• CONNECT


### 1.5.1 Safe und idempotent 

A safe operation is one which changes no state at all. Safe means that the request is intended only for information retrieval and should not change the state of the server
- 意思是：**不会对服务器资源造成改变的操作**。
- 常见的 **safe** 方法有：`GET`、`HEAD`    
- **DELETE 不是 safe 的**，因为它的目的是**删除资源**，显然会改变服务器状态。

An idempotent operation is one which updates state in an absolute way, and ==the result of executing the operation== (注意这里不是 response message 一致与否 ) more than once is the same as executing it once E.g. x = 4 rather than x += 2
- 意思是：**无论你调用一次还是多次，对服务器的结果都是一样的** 。
- `DELETE` 是 **幂等（idempotent）** 的。

假设有一个资源 `/api/item/123`：
- 第一次调用 `DELETE /api/item/123`：成功删除了资源
- 第二次调用 `DELETE /api/item/123`：==资源已不存在，返回 404 或无操作，但**服务器状态不会再次改变**==（还是没有这个资源）


Web-friendly systems scale because of safety
- Caching!

… and idempotent behavior benefits fault tolerance
- Just retry in failure cases


|方法|Safe|Idempotent|说明|
|---|---|---|---|
|GET|✅|✅|只读|
|POST|❌|❌|每次调用可能都创建新资源|
|PUT|❌|✅|多次更新结果一致|
|DELETE|❌|✅|多次删除效果一样|


### 1.5.2 Post 

Post semantics
- POST creates a new resource
    - The server decides on that resource’ URI
- Common human web example: posting a tweet
    - Server decides URI of tweet and any comments made on that tweet
- Programmatic web example: creating a new shopping cart and subsequently adding items to it


Post request
![[image/Pasted image 20250617125904.png]]

```
POST / HTTP/1.1
Content-Type: text/xml
Host: localhost:8888
Content-Length: ....
Connection: Keep-Alive
<buy>
    <symbol>ABCD</symbol>
    <price>27.39</price>
</buy>
```

POST response
```
201 CREATED
Location: /orders/halvards/ABCD/2007-07-080-13:50:53
```


We may get 4xx or 5xx errors
 Client versus server problem
We turn to GET!

Identify the resource state(s) first, then figure out how to make forward or backward progress, then solve the problem
 May involve POSTing again
 May involve a PUT to correct existing server-side resources


### 1.5.3 GET 


GET retrieves the representation of a resource
Should be safe
• Safe means that the request is intended only for information retrieval and should not change the state of the server
• In other words, GET should not have any side effects (beyond relatively harmless effects
such as logging, caching, the serving of banner advertisements or incrementing a web
counter)

```
GET /employees?id=1234 HTTP/1.1
Accept: text/xml
Host: crm.example.com
```

---


When GET goes wrong
Simple!
Just 404 – the resource is no longer available

Are you sure? GET again!

GET is safe (and hence also idempotent)
Consider crash recovery scenarios


### 1.5.4 PUT 

PUT semantics

PUT creates a new resource but the client decides on the URI, providing the server logic allows it

Also used to update existing resources by overwriting them in-place


---
PUT request

```
PUT /orders/halvards/ABCD/2007-07-080-13:50:53 HTTP/1.1
Content-Type: text/xml
Host: localhost:8888
Content-Length: ....
Connection: Keep-Alive

<buy>
<symbol>ABCD</symbol>
<price>44.42</price>
</buy>
```

![[image/Pasted image 20250617132036.png]]


---

PUT response
```
200 OK
Location: /orders/halvards/ABCD/2007-07-080-13:50:53
Content-Type: text/xml

<nyse:priceUpdated .../>
```


----

When PUT goes wrong

If we get 5xx errors, or some 4xx errors simply PUT again!
This is possible since PUT is idempotent
If we get errors indicating incompatible states (409, 417) then do some forward/backward
compensating work
And maybe PUT again

---

### 1.5.5 DELETE 

Stop the resource from being accessible
 Logical delete, not necessarily physical (data are often tombstoned): This is important for decoupling implementation details from resources   . 不是真正的删除 

Request
```
DELETE /user/halvards HTTP 1.1
Host: example.org
```



Response
```

200 OK
Content-Type: application/xml

<admin:userDeleted>
        halvards
</admin:userDeleted>
```



When DELETE goes wrong
DELETE again!
• Delete is idempotent
• DELETE once, DELETE 10 times has the same effect: (at most) one deletion

Some 4xx responses indicate that deletion is not possible
• The state of the resource is not compatible
• Try forward/backward compensation instead


## 1.6 More

Runtime aspects, including
• Persistent Connections
• Chunked Transfer
• Content Negotiation
• Caching
• Cookies
• …

Security with HTTPS:
• Replace port 80 with port 443
• Run secure TLS connection on top of TCP/IP
• Send HTTP requests via TLS connection

![[image/Pasted image 20250617132723.png]]


Coding example: HTTP calls with java.net.http
```
// Create a default client (e.g., default HTTP Method is GET)
HttpClient client = HttpClient.newHttpClient();
HttpRequest request = HttpRequest.newBuilder()
.GET() // HTTP Method
// You can also use URIBuilder from apache.httpcomponents to build more complicated URIs
.uri(URI.create("http://example.org"))
.header("Header-Name", "value")
// ... + lots more configuration options
.build();
// There are also more complicated BodyHandlers
HttpResponse<String> response = client.send(request, HttpResponse.BodyHandlers.ofString());
System.out.println(response.statusCode());
System.out.println(response.body())
```


# 2 Web APIs


An Application Programming Interface (API) is a way for multiple computer programs to communicate. If they communicate via the Web, it’s a Web API.
Modern (web) applications often use multiple web APIs, e.g., when embedding APIs for ads, maps, payment, social media, etc.
Web APIs are based on open standards – in the past, mostly based on SOAP/WSDL; today, mostly using REST.
But: There are no strict rules that describe how the interface of a Web API should look

Web APIs in a nutshell: A server exposes endpoints (usually HTTP(S)) with request-response interaction, often using JSON or XML as payload. The endpoints are called by clients, e.g., from a web browser (=> AJAX), mobile app, or a backend server.


SOAP/WSDL web services:
• First attempt at enabling web-based integration of remote services
• Still common in enterprise contexts (but not called from mobile or web apps)
• Can use arbitrary transport protocols
• Extensibility of enterprise-grade features (security, reliable messaging, routing, …)

- **首次尝试通过 Web 实现远程服务集成**：  
    SOAP（Simple Object Access Protocol）和 WSDL（Web Services Description Language）是早期用于系统之间远程通信的标准方式，最初用于解决跨平台系统之间如何远程调用函数的问题。
    
- **今天仍在企业环境中广泛使用**：  
    尽管它比较重，SOAP/WSDL 在大型企业的后端系统（如银行、保险、政府）中依然常见，但它们通常不会被移动或 Web 应用直接调用。
    
- **可使用任意传输协议（不止 HTTP）**：  
    与 REST 只能基于 HTTP 不同，SOAP 可以运行在多种协议上，比如 SMTP、JMS 等，具备更高的灵活性。
    
- **可扩展性强，支持企业级特性**：  
    比如安全性（WS-Security）、可靠消息传递（WS-ReliableMessaging）、路由控制等，SOAP 的一大特点就是支持这些复杂功能




RESTful services:
• Developed as lightweight alternative to SOAP/WSDL
• De facto standard today
• In essence: programmatically using HTTP-only, the way HTTP was intended to be used

- **作为 SOAP 的轻量替代方案出现**：  
    REST（Representational State Transfer）诞生于对 SOAP 的简化，它强调简单、无状态、资源导向，使用 HTTP 的标准方法（GET, POST, PUT, DELETE 等）。
    
- **已成为事实上的主流标准**：  
    目前，大多数现代 Web 和移动应用都使用 RESTful API 与后端进行通信。
    
- **本质上：使用 HTTP 的本来设计方式进行编程**：  
    REST 不是协议，而是一种架构风格，它将每个 URL 当作资源，并通过标准 HTTP 方法对资源进行操作，是最贴近 HTTP 初衷的用法。



GraphQL:
• Technology for getting data from RESTful APIs in a declarative way
**用于从 REST 接口中以声明式方式获取数据的技术**：  
GraphQL 由 Facebook 开发，它允许客户端根据自身需求**声明式**地请求数据（比如只要 name 和 email），而不是像 REST 那样依赖服务器提供固定的响应结构。


|比较维度|SOAP/WSDL - XML|REST - JSON|
|---|---|---|
|格式/风格|重/正式/结构严格|轻量/灵活/结构简单|
|扩展性|高（支持各种复杂功能）|低（但也因此简单）|
|易用性|学习和实现成本高|更易开发，社区支持广泛|
|使用场景|企业级、规范严格的后端系统|Web、移动端通信，现代微服务架构等|



- 企业后端遗留系统 ➜ **SOAP/WSDL**
- 日常 Web / App 开发 ➜ **REST*    
- 需要前端定制查询效率（如 GraphQL Playground）➜ **GraphQL**





### 2.1.1 SOAP/WSDL web services and the WS-* platform

==SOAP mittlerweile für nichts mehr, WSDL ist die Web Service Description Language==



SOAP was originally created in 1999 (as “XML-RPC”). Since 2001 the W3C has been working on SOAP (“Simple Object Access Protocol”). Since 2003 SOAP no longer is an acronym. Web services can communicate over the Internet by exchanging SOAP messages. While SOAP is protocol-agnostic, it is often implemented by sending XML messages via HTTP

Remember gRPC? • SOAP/WSDL is an XML-based, more heavyweight alternative with more enterprise-grade features • It is conceptually roughly the same: ~ WSDL is the IDL, SOAP describes the message format and the interaction


SOAP/WSDL are part of the WS-* platform. The set of WS-* standards can be used to add additional capabilities to SOAP/WSDL web services, e.g., reliable messaging, addressing, security, transactions, …


![[image/Pasted image 20250619145325.png]]



## 2.2 SOAP 


### 2.2.1 Exchanging SOAP messages: high-level overview 

基于 rpc 

![[image/Pasted image 20250619145524.png]]


![[image/Pasted image 20250619145635.png]]



### 2.2.2 what is SOAP? 
1. A message format for one-way communication describing how a message can be packed into an XML document 
2. A description of how the message (the XML document) should be transported using HTTP (for Web based interaction), SMTP (for e-mail based interaction), or other transport protocols 
3. A set of rules that must be followed when processing a SOAP message and a simple classification of the entities involved in processing a SOAP message. It also specifies what parts of the messages should be read by whom and how to react in case the content is not understood. 
4. A set of conventions on how to turn an RPC call into a SOAP message and back, as well as how to implement the RPC style of interactio


### 2.2.3 SOAP Messages 

SOAP messages are seen as envelopes where the application encloses the data to be sent A message has two main parts:- Header (divided into blocks)- Body (also divided into blocks) SOAP does not say what to do with these parts, it only states that - the header is optional (often used for infrastructure-level data)- the body is mandatory (often used for application-level data)

![[image/Pasted image 20250619150039.png]]



---


RPC style vs. documentstyle
The SOAP bodycancontaindataeitherin RPC style orin documentstyle.
RPC style:
• Directlyequivalenttoan RPC call
• Body containsan elementwiththefunctionbeingcalled(and childelementsforeachoftheparameters)

Documentstyle:
• More similartomessagingwithstand-alone, self-explainingpayload
• Recipientneedstoknowhowtoprocessthepayload
• Nospecificruleson whatthebodyshouldlooklike


## 2.3 WSDL









WSDL Summary

WSDL includes a grammar and syntax on top of XML Schema to describe Web services as a set of communication endpoints. A complete WSDL definition contains all necessary informationto invoke a web service.

The databeing exchanged between the endpoints is specified as part of messagesand every kind of processing activity allowed at an endpoint is considered as an operation.

The WS-* set of standards supports many different extensions to the “simple” use case: for example, success answers and error messages could be sent to different receivers.




## 2.4 SOAP/WSDL differ from gRPC

- **Use SOAP/WSDL** when you need **strict contracts, enterprise features, or legacy system integration**.
    
- **Use gRPC** when you need **high performance, low latency, streaming, and modern microservice communication**.





## 2.5 REST


The concepts of REST were developed by Roy Fielding in 2000 and are generally used as the standard for web APIs today.

General Idea: The web is a scalable and reliable system -> formalize this style and use it for future system. Use HTTP, the basis of the web, also for programmatic API interaction.


New concept:
• Everything is an (abstract) resource which we can modify (=> CRUD)
• The implementation decides how the change of the resource is reflected in server data.

### 2.5.1 REST constraints

Roy Fielding defined six constraints for the REST architectural style. 
A system that fulfils these requirements gains non-functional properties such as performance, scalability, simplicity, modifiability, portability, and reliability:
- Client-server Architecture
- Statelessness
- Cacheability
- Layered System
- Uniform Interface
- Code on demand

In practice, some of them are relevant to REST, others only for the part of the web directly interacting with humans.


---

1  Client-server architecture
Separation of Concerns: Separate the user interface from the data storage. This allows the server and clients to evolve separately.
To implement this, we use web browsers to present information to users, and use web APIs to deliver data from servers to clients.


---

2 Statelessness
不能用 session 但是能用  cookies 去满足 stateless 的这个条件 
Use a communication protocol in which the receiver does not need to retain session information from previous messages.

I.e., every new message can be processed by a different application server. If the client needs state, the request must contain all information necessary to respond to the request.

We can implement this using HTTP Cookies, which are sent with every request.

⇒ The interaction itself does not hold state (there is no concept of a session)
⇒ State is only held by the client (and to some degree by the server in the form of resources)


---
3 






